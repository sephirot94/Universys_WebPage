function redirectorSearch(json, mapa) {
    var codigo = json.error-code;
    for(var key in mapa) {
        if()codigo==key) {
            window.location.href = //key[value] devolver value referido de la key ;
        }
    }
}